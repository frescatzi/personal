# wiki/ — pages propres (personal, écrites par le LLM)

Résumés, concepts, synthèses perso/business/culture générale.
- Frontmatter `type: wiki`, `vault: personal`.
- Par défaut `publish: none` (contenu potentiellement sensible).
- N'atteint les agents que si `status: active` ET `publish: notion`.
